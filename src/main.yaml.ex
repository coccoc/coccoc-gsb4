# fill your api key here and rename/copy main.yaml.ex to main.yaml to make it works
api_key: <fill your api key here>

# folder which contains raw data
storage: /etc/coccoc-gsb4/data/

# folder which contains extracted data so human and java can read easily
output: /etc/coccoc-gsb4/extract/

lock_file: /tmp/coccoc.gsb4.lock